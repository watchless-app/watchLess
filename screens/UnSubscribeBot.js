import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const UnSubscribeBot = () => {
  return (
    <View style={styles.container}>
      <Text>Coming Soon!</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    margin: 10,
  },
});

export default UnSubscribeBot;
